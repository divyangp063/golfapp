import React from "react";

const ContactUs = () => {
    let title = "Contact Us page";

    return (
        <div>
            <h1>
                {title}
            </h1>
        </div>
    );
};

export default ContactUs;