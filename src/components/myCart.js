import React from "react";

const MyCart = () => {
    let title = "My Cart page";

    return (
        <div>
            <h1>
                {title}
            </h1>
        </div>
    );
};

export default MyCart;