import React from "react";

const Home = () => {
    let title = "Home page";

    return (
        <div>
            <h1>
                {title}
            </h1>
        </div>
    );
};

export default Home;